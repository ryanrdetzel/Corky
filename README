Corky 0.1
    
Requires: Python 2.6 or greater (I think)

To Run:
    1. Make sure you have everything required
    2. run python corky.py debug - if the server starts you should be good to go, if not fix what's wrong
    3. run python corky.py start  to start corky, visit http://YOURIP:8000
    4. To stop or restart(new plugins installed) using python corky.py [stop|restart]
